# Master Codex Build Prompt

Use the prompt package provided by product leadership (see task brief) to drive full implementation in Codex code mode.

## Immediate sequence
1. Scaffold monorepo
2. Add shared types + scheme engine + registry
3. Implement OTP flow with rate limits
4. Build mobile onboarding flow (OTP → Aadhaar step → profile → results)
5. Add persistence + matching APIs
6. Build admin dashboard
7. Add tests + docs + health checks
8. Run lint/typecheck/test/build
